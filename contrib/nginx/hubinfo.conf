# Pinger endpoint. Goes in conf.d, inside the existing http {} block.
#
# PtokaX rewrites this file every 60 seconds while PingerAddresses is set.

location = /api/v0/hubinfo.json {
	alias @STATEDIR@/hubinfo.json;
	default_type application/json;
	add_header Cache-Control "max-age=60";
}
